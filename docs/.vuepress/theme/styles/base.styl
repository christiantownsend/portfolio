@font-face {
    font-family: 'PP Neue Montreal';
    font-weight: normal;
    font-style: normal;
    src: url('/static/fonts/PPNeueMontreal-Regular.woff2');
}

@font-face {
    font-family: 'PP Neue Montreal';
    font-weight: 500;
    font-style: normal;
    src: url('/static/fonts/PPNeueMontreal-Medium.woff2');
}

@font-face {
    font-family: 'PP Neue Montreal';
    font-weight: normal;
    font-style: italic;
    src: url('/static/fonts/PPNeueMontreal-Italic.woff2');
}

@font-face {
    font-family: 'PP Neue Montreal';
    font-weight: bold;
    font-style: normal;
    src: url('/static/fonts/PPNeueMontreal-Bold.woff2');
}

@font-face {
    font-family: 'PP Neue Montreal';
    font-weight: bold;
    font-style: italic;
    src: url('/static/fonts/PPNeueMontreal-BoldItalic.woff2');
}

@font-face {
    font-family: 'TWK Lausanne';
    font-weight: 300;
    font-style: normal;
    src: url('/static/fonts/TWKLausanne-300.woff2');
}

@font-face {
    font-family: 'TWK Lausanne';
    font-weight: 500;
    font-style: normal;
    src: url('/static/fonts/TWKLausanne-500.woff2');
}

@font-face {
    font-family: 'TWK Lausanne';
    font-weight: 500;
    font-style: italic;
    src: url('/static/fonts/TWKLausanne-500Italic.woff2');
}

@font-face {
    font-family: 'TWK Lausanne';
    font-weight: bold;
    font-style: normal;
    src: url('/static/fonts/TWKLausanne-800.woff2');
}

@font-face {
    font-family: 'TWK Lausanne';
    font-weight: bold;
    font-style: italic;
    src: url('/static/fonts/TWKLausanne-800Italic.woff2');
}

::selection {
    background: var(--color-primary);
    color: var(--color-neutral-900);
}

::moz-selection {
    background: var(--color-primary);
    color: var(--color-neutral-900);
}

html {
    scrollbar-color: var(--color-neutral-100-t) var(--color-neutral-900);
}

::-webkit-scrollbar-thumb {
    background: var(--color-neutral-100-t);
}

::-webkit-scrollbar-track {
    background: var(--color-neutral-900);
}

html * {
    font-family: var(--font-stack);
    text-rendering: optimizeLegibility;
}

body{
    background: var(--color-neutral-900);
    color: var(--color-neutral-100);
    font-size: var(--font-size);
    font-weight: var(--font-weight);

    transition: color 0.5s, background-color 0.5s;
    overflow-x: hidden;
}

hr {
    margin: 0;
    // opacity: var(--lightened-opacity);
    opacity: .25;
    color: var(--color-neutral-100);
    display: block;
    border: none;
    height: 1px;
    background: currentColor;
}

#app > div:is(:first-child) {
    min-height: 100vh
    display: flex;
    justify-content: flex-start;
    flex-direction: column;
}


.container {
    padding-inline: @css{min(3rem, 8%)};
}

video {
    width: 100%;
}

.content__default {
    // position: absolute;

    img, picture {
        width: 100%;
    }

    a {
        text-decoration: underline;
        text-decoration-skip-ink: all;
        text-decoration-thickness: .08em;
        text-underline-offset: .15em;
        text-decoration-color: var(--color-primary);
    }
}

.grid {
    display: grid;
    grid-template-columns: repeat(12, minmax(0, 1fr));
    grid-auto-rows: minmax(min-content, max-content);
    white-space: normal;
    // min-width: 0;
}

.grid > *:not([class*=span]) {
    grid-column-start: span 12;
}

a {
    color: inherit;
    text-decoration: none;
    display: inline;
    // white-space: pre;
}

a:not(.no-arrow) {
    --arrow-spacing: .25em;

    padding-right: calc(1ch + var(--arrow-spacing));
    display: inline-block;
}

a:not(.no-arrow)::after {
    display: inline-block;
    content: '↗';
    padding-left: var(--arrow-spacing);
    width: 1ch;
    margin-right: -1ch;
    transition: transform .5s var(--default-timing);
}

a:hover::after, a:hover a::after {
    transform: translateX(.25em);
}

.icon.outbound {
    display: none;
}

ul {
    margin: 0;
    padding: 0;

    li {
        margin-bottom: 1em;
    }
}

.pill {
    text-transform: uppercase;
    padding: .15em 1.25em;
    border-radius: 100px;
    border: 1px solid var(--color-neutral-100);
    // border 1px solid currentColor;
    transition: all .25s var(--default-timing);
}

// .pill:hover {
//     color: var(--color-neutral-900);
//     background: var(--color-neutral-100);
//     transform: scale(1.1);
// }

.small {
    font-size: .85em
}

[data-tag] {
    position: relative;
}

[data-tag]::before {
    // --rotation: attr(data-tag-rotation);

    content: attr(data-tag);
    z-index: 5;
    position: absolute;
    display: block;
    width: min-content;
    aspect-ratio: 1 / 1;
    font-size: var(--font-size);
    line-height: .85;
    padding: .6em;

    display: flex;
    align-items: center;
    
    text-transform: uppercase;
    color: var(--color-neutral-900);
    background: var(--color-neutral-100);
    border-radius: 50%;
    // border: 1px solid currentColor;
}

[data-tag][data-tag-pos=top-left]::before {
    top: 0;
    left: 0;
    transform: translate(-35%, -35%) rotate(var(--rotation));
}
[data-tag][data-tag-pos=top-right]::before {
    top: 0;
    right: 0;
    transform: translate(35%, -35%) rotate(var(--rotation));
}
[data-tag][data-tag-pos=bottom-left]::before {
    bottom: 0;
    left: 0;
    transform: translate(-35%, 35%) rotate(var(--rotation));
}
[data-tag][data-tag-pos=bottom-right]::before {
    bottom: 0;
    right: 0;
    transform: translate(35%, 35%) rotate(var(--rotation));
}